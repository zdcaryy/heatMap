import { ScalingDirective } from './scaling.directive';

describe('ScalingDirective', () => {
  it('should create an instance', () => {
    const directive = new ScalingDirective();
    expect(directive).toBeTruthy();
  });
});
